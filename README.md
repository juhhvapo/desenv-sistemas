git clone https://github.com/juhhvapo/desenv-sistemas

git config --global user.name "Júlia"

git config --global user.email "jsm581160@gmail.com"

git add .

git commit -m "escreva a mensagem"

git push
